source:
  type: csv-enricher
  config:
    filename: /workspace/metadata/datahub_metadata_update.csv
    write_semantics: PATCH  # 기존 메타데이터에 추가
    delimiter: ","
    array_delimiter: "|"
    encoding: "utf-8"

sink:
  type: datahub-rest
  config:
    server: "http://datahub-gms:8080"
